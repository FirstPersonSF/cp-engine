"""Command modules for the `cp` CLI (arch-phase-4, issue #33).

Each module defines flat `click` commands; `cp_engine.cli` assembles
them onto the single top-level group. User-facing command names are
IDENTICAL to the pre-split CLI.
"""
