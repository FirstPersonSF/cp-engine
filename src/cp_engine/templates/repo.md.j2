---
Project: {{ project.name }}
Provenance: cp-engine v{{ engine_version }} | {{ today }}
Filename: _repo.md
Author: cp-engine (generated)
---

# Source repository

[{{ project.github_org }}/{{ project.repo_name }}](https://github.com/{{ project.github_org }}/{{ project.repo_name }})

This directory holds the collaboration layer for this project —
context document (`cp.md`), session transcripts, AI-generated
syntheses, planning notes. The actual source code lives in the
linked GitHub repo.

If you have the repo cloned locally, work on the code there
(`cd ~/path/to/{{ project.repo_name }}`). If not, GitHub is the
canonical source. Either way, this directory is where the team
thinks together about the project.
