{
  "description": "SessionStart hooks: auto-install the matching cp CLI when the plugin and CLI versions drift; tenant-clone freshness gate (fetch always, ff-only when safe, loud warning otherwise).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/sync-cli-version.sh",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/tenant-freshness.sh",
            "timeout": 25
          }
        ]
      }
    ]
  }
}
