{
  "description": "SessionStart: ONE entry. Claude Code runs same-matcher hooks in parallel with no ordering guarantee, so the two scripts are chained by session-start.sh in a fixed order: CLI-version observe/sync, then the tenant-clone freshness gate (#296).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
